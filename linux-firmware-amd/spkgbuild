# description	: Firmware files for Linux

name=linux-firmware
version=20230404
release=1
source="https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git/snapshot/linux-firmware-$version.tar.gz"

build() {
	cd $name-$version
	
	install -Dm755 $PKG/lib/firmware
	cp -r amd amd-ucode rtl_nic $PKG/lib/firmware/

	install -Dm755 $PKG/lib/firmware/amdgpu
	cp amdgpu/renoir* $PKG/lib/firmware/amdgpu/
}
